#include <stdio.h>

main()
{
  int i, j, k;
  for (i=0 ; i<=N ; i++)
    p[i] = 1;
  for (i=2 ; i<=N ; i++)
  {
    k = p[i]; j=1;
    while (a[p[j-1]] > a[k] {
        p[j] = p[j-1];
        j--;
    }
    p[j]=k;
  }
for(int i=0;i<N;i++){
  switch(N){
    case 1:
        printf("one");
    case 2:
        printf("two");
    case 10:
        printf("ten");
        break;
    case 12:
        printf("twelve");
        break;
    default:
        printf("none");
  }
  }
 }