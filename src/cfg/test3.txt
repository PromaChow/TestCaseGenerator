#include <stdio.h>

#include <stdio.h>

int main( )
{
	int num;
	scanf("%d", &num);

	if(num>=10){
	    printf("The number is greater than 10 \n");
	}
	else
	    printf("The number is less than 10");
}